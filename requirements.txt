# Python dependencies for browser-use skill
# Install: pip3 install -r requirements.txt

mss>=9.0.0                      # Fast screenshots
pyautogui>=0.9.54               # OS-level mouse/keyboard control
Pillow>=10.0.0                  # Image handling (screenshots, OCR input)
rapidocr-onnxruntime>=1.3.0     # OCR engine (on by default)

# LocateAnything-3B local mode (optional — only for `find` without API):
#   pip3 install mlx-vlm
#   huggingface-cli download nvidia/LocateAnything-3B
# If not installed, `find` uses the hosted API automatically (requires pave login).
